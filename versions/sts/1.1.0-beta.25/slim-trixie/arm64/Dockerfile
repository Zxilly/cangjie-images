
# syntax=docker/dockerfile:1.7
# Generated by cangjie-images. Do not edit by hand.
# channel: sts
# version: 1.1.0-beta.25
# arch: arm64

FROM scratch AS sdk
ADD --checksum=sha256:736b104fa35249fdb913786454db0cef2c13418452eba901a12a0214c2f227f9 \
    https://cangjie-lang.cn/v1/files/auth/downLoad?nsId=142267&fileName=cangjie-sdk-linux-aarch64-1.1.0-beta.25.tar.gz&objectKey=69cb74a76e8ed61e6e07fd45 \
    /sdk.tar.gz

FROM debian:bookworm-slim AS extractor
RUN --mount=from=sdk,source=/sdk.tar.gz,target=/sdk.tar.gz <<'EOF'
set -eux
mkdir -p /out/opt
tar -xzf /sdk.tar.gz -C /out/opt \
      --exclude='cangjie/lib/windows_*' \
      --exclude='cangjie/runtime/lib/windows_*' \
      --exclude='cangjie/modules/windows_*' \
      --exclude='cangjie/third_party/mingw' \
      --exclude='cangjie/lib/*.dll' \
      --exclude='cangjie/lib/*.dll.a'
EOF

FROM debian:trixie-slim
RUN <<'EOF'
set -eux
export DEBIAN_FRONTEND=noninteractive
apt-get update
apt-get install -y --no-install-recommends \
      bash \
      binutils \
      ca-certificates \
      curl \
      findutils \
      g++ \
      gcc \
      git \
      libc6-dev \
      libssl-dev \
      make \
      openssl \
      pkg-config \
      procps \
      tar \
      unzip \
      xz-utils \
      zip
rm -rf /var/lib/apt/lists/*
EOF

COPY --from=extractor /out/ /

ENV CANGJIE_HOME="/opt/cangjie"
ENV LD_LIBRARY_PATH="/opt/cangjie/runtime/lib/linux_aarch64_cjnative:/opt/cangjie/tools/lib"
ENV PATH="/opt/cangjie/bin:/opt/cangjie/tools/bin:$PATH:/root/.cjpm/bin"

LABEL org.opencontainers.image.title="Cangjie 1.1.0-beta.25 (slim-trixie/arm64)"
LABEL org.opencontainers.image.description="Prebuilt Cangjie STS 1.1.0-beta.25 SDK on slim-trixie (arm64, cjnative backend)"
LABEL org.opencontainers.image.version="1.1.0-beta.25"
LABEL org.opencontainers.image.source="https://github.com/Zxilly/cangjie-images"
LABEL org.opencontainers.image.url="https://github.com/Zxilly/cangjie-images"
LABEL org.opencontainers.image.documentation="https://github.com/Zxilly/cangjie-images#readme"
LABEL org.opencontainers.image.vendor="Zxilly"
LABEL org.opencontainers.image.licenses="Apache-2.0"
LABEL org.opencontainers.image.base.name="debian:trixie-slim"
LABEL io.cangjie.channel="sts"
LABEL io.cangjie.version="1.1.0-beta.25"
LABEL io.cangjie.arch="arm64"
LABEL io.cangjie.backend="cjnative"
LABEL io.cangjie.base="slim-trixie"
LABEL io.cangjie.base.family="debian"
LABEL io.cangjie.sdk.url="https://cangjie-lang.cn/v1/files/auth/downLoad?nsId=142267&fileName=cangjie-sdk-linux-aarch64-1.1.0-beta.25.tar.gz&objectKey=69cb74a76e8ed61e6e07fd45"
LABEL io.cangjie.sdk.sha256="736b104fa35249fdb913786454db0cef2c13418452eba901a12a0214c2f227f9"

WORKDIR /workspace
CMD ["bash"]
